#include "holberton.h"
/**
 * print_most_numbers- print digits of 0 to 9.
 */
void print_most_numbers(void);
{
int e;
for (e = '0'; e <= '9'; e++)
{
if (e != 2 && e != 4)
_putchar(e);
}
_putchar ('\n');
}
